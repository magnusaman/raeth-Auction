services:
  raeth:
    build: .
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=file:/app/data/raeth.db
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - raeth-data:/app/data
    restart: unless-stopped

volumes:
  raeth-data:
